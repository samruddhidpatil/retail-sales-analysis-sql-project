name: SQL project CI

on:
  push:
    branches:
      -main
jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "My first github actions job is running"
